{
  "name": "cachedigitech-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cache Digitech website - Hostinger Node.js Web App",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node server.js",
    "preview": "npm run build && node server.js"
  },
  "dependencies": {
    "compression": "^1.8.0",
    "express": "^4.21.2"
  }
}
